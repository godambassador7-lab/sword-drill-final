Optional local ESV per‑book files (if you split your ESV JSON).

Note: ESV is licensed. The app already supports the official ESV API. If you have a local JSON for personal use, you can split it with:

node scripts/splitBibleJson.js src/data/ESV_bible.json esv

Files expected: Genesis.json, Exodus.json, ... Revelation.json

